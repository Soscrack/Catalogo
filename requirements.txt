# Catálogo Ferretería - Dependencias de Python
# Instalar con: pip install -r requirements.txt

pandas>=1.5.0       # Manipulación de DataFrames
openpyxl>=3.0.0     # Lectura/escritura de archivos Excel (.xlsx)
pyyaml>=5.4         # Carga de archivos YAML para configuración

# Extracción de catálogo desde PDF
pymupdf>=1.24.0      # Lectura de PDF (PyMuPDF/fitz)
# Opcional: extracción espacial con LLMWhisper (guardar .txt para no repetir llamadas)
# pip install llmwhisperer-client
# Variables de entorno: LLMWHISPERER_API_KEY, LLMWHISPERER_BASE_URL_V2

# Scraping / deploy / import remoto
requests>=2.28.0
paramiko>=3.0.0
pymysql>=1.0.0
python-dotenv>=1.0.0

# Opcional para desarrollo
pytest>=7.0         # Testing
black>=22.0         # Formateo de código
